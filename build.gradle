allprojects {

    group 'io.rsocket.tck'
    version '0.0.1-SNAPSHOT'

    repositories {
        jcenter()
        mavenCentral()
    }

    tasks.withType(GenerateModuleMetadata) {
        enabled = false
    }

}
